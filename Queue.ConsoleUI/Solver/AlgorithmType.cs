namespace Queue.ConsoleUI.Solver
{
    // Do NOT explicitly assign number values to this enum
    // as the UI implementation relies on the natural count
    // starting from 0
    internal enum AlgorithmType
    {
        Jackson,
        BcmpOne,
        BcmpThree
    }
}